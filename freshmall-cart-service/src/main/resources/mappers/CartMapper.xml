<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.tedu.cart.mapper.CartMapper">
   <select id="selectCartByUserId" parameterType="String" resultType="Cart">
   select * from cart where user_id=#{userId};
   </select>
   
   <select id="selectCartByUserIdAndProductId" resultType="Cart">
   select * from cart where user_id=#{userId} and pid=#{pid}
   </select>
   
   <update id="updateNumByUserIdAndProductId" parameterType="Cart">
   update cart set num=#{num} where user_id=#{userId} and pid=#{pid};
   </update>
   
   <insert id="insertCart" parameterType="Cart">
   insert into cart (user_id,pid,pname,pphoto,phyprice,num) values (#{userId},#{pid},#{pname},#{pphoto},#{phyprice},#{num})
   </insert>
   
   <delete id="deleteByUserIdAndProductId" parameterType="Cart">
   delete from cart where user_id=#{userId} and pid=#{pid};
   </delete>
</mapper>